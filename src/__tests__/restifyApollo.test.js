/**
 * Basic Restify test
 */

// import {  } from '../lib/';
describe('Apollo Restify integration base test', () => {
    it('Adds an Apollo server endpoint', () => {
        expect(true);
    });
});
