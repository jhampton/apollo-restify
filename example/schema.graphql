type Query {
  me: User
  user: User
}

type User {
  id: ID!
  name: String
  username: String
  posts: [Post]
}


type Post {
    id: ID!
    title: String
    body: String
    bodyIsHtml: Boolean
}